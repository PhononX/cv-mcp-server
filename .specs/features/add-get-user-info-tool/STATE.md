# STATE — add-get-user-info-tool

> Auto-generated by the /dev workflow. These items did NOT block the PR but need human
> review before the feature is considered validated. This file is the source of truth for validation.

## ℹ️ LOW findings — informational, not auto-fixed (2)
- /Users/fredericoalmeida/Dev/phononx/cv-mcp-server-add-get-user-info-tool/src/cv-api.ts: Contact interface borrows workspace_roles shape from UserInfo['workspace_roles'] rather than defining it independently. Creates a semantic inversion (input type referencing output type) though it type-checks cleanly and causes no runtime issues.
- /Users/fredericoalmeida/Dev/phononx/cv-mcp-server-add-get-user-info-tool/tests/unit/server/server.test.ts: Duplicate top-level mock declarations (mockRegisterTool at line 11 and 89, McpServer at lines 14-19 and 92-94). The outer jest.mock calls are dead code superseded by jest.doMock inside beforeEach. Pre-existing pattern not introduced by this PR but worth noting for future cleanup.
