# STATE — add-get-user-info-tool

> Auto-generated by the /dev workflow. These items did NOT block the PR but need human
> review before the feature is considered validated. This file is the source of truth for validation.

## ✅ All LOW findings resolved

- `src/cv-api.ts`: `Contact.workspace_roles` now typed as `WorkspaceRole[]` directly — semantic inversion removed.
- `tests/unit/server/server.test.ts`: Removed dead outer `mockRegisterTool` declaration and its `jest.mock` block (lines 11–19) that were superseded by `jest.doMock` in `beforeEach`. All 89 tests pass.
